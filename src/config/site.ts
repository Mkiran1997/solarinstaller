// Fill these in before you send/deploy the demo. Per the brief: plain text,
// not a live/computed date (e.g. do NOT use `new Date()` here).
export const UPWORK_NAME = 'YOUR_UPWORK_NAME'
export const FINISHED_ON = 'YYYY-MM-DD'
